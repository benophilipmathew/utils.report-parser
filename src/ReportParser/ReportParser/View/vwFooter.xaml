<UserControl x:Class="ReportParser.View.vwFooter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="250">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Style="{StaticResource FooterTextB}" HorizontalAlignment="Stretch">
            <TextBlock Style="{StaticResource FooterText}" Text="{Binding FooterTitle}"></TextBlock>
        </Border>
        <Border Style="{StaticResource FooterLogoB}">
            <Image Source="/ReportParser;component/Resources/images/ReportParserLogo.png" />
        </Border>
    </Grid>
</UserControl>
