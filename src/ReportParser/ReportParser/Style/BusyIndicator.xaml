<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls">

    <Style x:Key="BIOverlay" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SecondaryLightCB}" />
        <Setter Property="Opacity" Value="0.8" />
    </Style>
    
    <Style x:Key="ProgressRing" TargetType="controls:ProgressRing">
        <Setter Property="Foreground" Value="{StaticResource SecondaryLightCB}" />
        <Setter Property="Margin" Value="10" />
        <Setter Property="Width" Value="180" />
        <Setter Property="Height" Value="230" />
    </Style>

    <Style x:Key="ProgressDetail" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource SecondaryLightCB}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="Margin" Value="15px 0 0 30px" />
        <Setter Property="FontSize" Value="12px" />
        <Setter Property="FontFamily" Value="calibri" />
        <Setter Property="FontStyle" Value="Italic" />
        <Setter Property="Foreground" Value="{StaticResource SecondaryDarkCB}" />
        <Setter Property="Opacity" Value="0.8" />
    </Style>

</ResourceDictionary>